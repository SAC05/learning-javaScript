const nombre = 'monitor 20 pulgadas';
const disponible = true;
const precio = 300;

//Un objeto agrupa toda la información en una sola variable

//Object literal 
const producto = {
    nombre: 'monitor 20 pulgadas',
    precio: 300,
    disponible: true,
}
console.log(producto);
