const meses = ['Enero','Febrero','Marzo','Abril','Mayo','Junio', 'Julio'];

//Utilizamos .length para saber cuanto mide el arreglo
console.log(meses.length);

for(let i = 0; i < meses.length; i++) {
    console.log( meses [i] );
}